/** Shared so the login and register forms cannot drift apart. */

/** Flat grey field, no visible label — the placeholder does that job. */
export const FIELD =
  'h-11 rounded-md border-0 bg-muted px-4 placeholder:text-muted-foreground focus-visible:ring-2 focus-visible:ring-[#644fef]/40';

/** Full-width brand pill. */
export const SUBMIT =
  'mt-3 h-11 w-full rounded-full bg-[#644fef] text-xs font-semibold uppercase tracking-wider text-white hover:bg-[#5540d8]';
